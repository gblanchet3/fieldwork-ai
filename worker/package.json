{
  "name": "fw-training-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Fieldwork training backend — Claude proxy + capture (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "wrangler": "^3.86.0"
  }
}
